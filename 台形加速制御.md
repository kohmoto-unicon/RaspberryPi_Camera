# 台形加速制御仕様書
**バージョン**: 1.0  
**作成日**: 2025年10月8日  
**対象ファイル**: `arduino/kousoku5/kousoku5.ino`

---

## 目次
1. [概要](#概要)
2. [設計思想](#設計思想)
3. [主要変数・定数](#主要変数定数)
4. [制御フロー](#制御フロー)
5. [台形プロファイル計算](#台形プロファイル計算)
6. [割り込み処理による速度制御](#割り込み処理による速度制御)
7. [コマンド処理](#コマンド処理)
8. [事前計算配列（無効化）](#事前計算配列無効化)
9. [制限事項と注意点](#制限事項と注意点)

---

## 概要

### 台形加速制御とは
台形加速制御は、ステッピングモーターを滑らかに加速・減速させるための速度制御方式です。速度プロファイルが台形（または三角形）になることからこの名称がつけられています。

### 速度プロファイル
```
速度
  ^
  |      +-----------+  台形（等速区間あり）
  |     /|           |\
  |    / |           | \
  |   /  |           |  \
  |  /   |           |   \
  | /    |           |    \
  +--------------------------> 時間
    加速   等速      減速
```

### 本システムの特徴
- **3軸独立制御**: 3台のモーターを個別に台形加速制御可能
- **動的プロファイル計算**: ステップ数と目標速度から最適な加減速プロファイルを自動計算
- **三角プロファイル対応**: ステップ数が少ない場合は自動的に三角プロファイルに切り替え
- **リアルタイム速度更新**: 割り込み処理内で高精度な速度制御を実現

---

## 設計思想

### 1. 2段階制御方式
台形加速制御は以下の2段階で実行されます：

1. **事前計画フェーズ** (Mコマンド受信時)
   - 総ステップ数、目標速度、加速度から加減速プロファイルを事前計算
   - 加速ステップ数、等速ステップ数、減速ステップ数を決定

2. **実行フェーズ** (タイマー割り込み内)
   - 現在のステップ数に応じて速度を更新
   - 浮動小数点演算による高精度な速度制御

### 2. 無限動作と有限動作
- **有限動作** (`remainingSteps > 0`): 事前計画に基づく台形/三角プロファイル
- **無限動作** (`remainingSteps = 0`): 目標速度まで加速後、等速を維持

### 3. 最小開始速度の導入
- モーターの脱調防止のため、最小開始速度 `minStartSpeedSps = 650.0 [steps/s]` から動作開始
- 停止時も最小開始速度まで減速してから停止

---

## 主要変数・定数

### グローバル定数

```cpp
// 基本パラメータ
const float minStartSpeedSps = 650.0f;        // 最小開始速度 [steps/s]
const float targetRampTimeSec = 0.2f;         // 加速時間目標値 [s]
const int stepsPerRev = 400;                  // 1回転あたりのステップ数
```

### モーター毎の制御変数 (配列: idx=0-2)

#### 台形加速ON/OFF制御
```cpp
volatile bool useTrapezoid[3] = {false, false, false};  // 台形加速の有効/無効
```

#### 現在の速度状態
```cpp
volatile float currentSpeedSps[3] = {0.0f, 0.0f, 0.0f}; // 現在速度 [steps/s]
volatile float targetSpeedSps[3]  = {0.0f, 0.0f, 0.0f}; // 目標速度 [steps/s]
volatile float accelerationSps2[3] = {4000.0f, 4000.0f, 4000.0f}; // 加速度 [steps/s^2]
```

#### タイマー制御
```cpp
volatile unsigned int stepInterval[3] = {0, 0, 0};      // ステップパルス間隔 [μs]
volatile bool motorEnabled[3] = {false, false, false};  // モーター動作中フラグ
volatile unsigned long remainingSteps[3] = {0, 0, 0};   // 残りステップ数 (0=無限)
```

#### 事前計画変数
```cpp
volatile bool planActive[3] = {false, false, false};            // 事前計画の有効/無効
volatile unsigned long planTotalSteps[3] = {0, 0, 0};          // 計画総ステップ数
volatile unsigned long planStepsDone[3]  = {0, 0, 0};          // 実行済みステップ数
volatile unsigned long planAccelSteps[3] = {0, 0, 0};          // 加速フェーズのステップ数
volatile unsigned long planCruiseSteps[3]= {0, 0, 0};          // 等速フェーズのステップ数
volatile unsigned long planDecelSteps[3] = {0, 0, 0};          // 減速フェーズのステップ数
volatile float planPeakSpeedSps[3]       = {0.0f, 0.0f, 0.0f}; // ピーク速度 [steps/s]
```

---

## 制御フロー

### 全体フロー図

```
[Aコマンド: 台形加速ON]
          ↓
  useTrapezoid[idx] = true
          ↓
[Vコマンド: 速度設定]
          ↓
  targetSpeedSps[idx] = 指定速度
  accelerationSps2[idx] = (速度差) / 0.2秒
          ↓
[Mコマンド: モーター開始]
          ↓
  ┌─────────────────┐
  │事前計画フェーズ      │
  │- 加速ステップ数計算  │
  │- 等速ステップ数計算  │
  │- 減速ステップ数計算  │
  │- ピーク速度決定      │
  └─────────────────┘
          ↓
  planActive[idx] = true
  currentSpeedSps[idx] = minStartSpeedSps
          ↓
  ┌─────────────────┐
  │タイマー開始          │
  └─────────────────┘
          ↓
  ┌─────────────────┐
  │割り込み処理（高速）  │ ← ループ
  │- ステップパルス生成  │
  │- 速度更新            │
  │- フェーズ判定        │
  └─────────────────┘
          ↓
  remainingSteps[idx] == 0
          ↓
  モーター停止
```

---

## 台形プロファイル計算

### 計算タイミング
Mコマンド受信時（1177-1228行目）に実行されます。

### 計算アルゴリズム

#### 入力パラメータ
- `total`: 総ステップ数 (`value * 2`)
- `vtar`: 目標速度 [steps/s] (`targetSpeedSps[idx]`)
- `a`: 加速度 [steps/s^2] (`accelerationSps2[idx]`)
- `v0`: 開始速度 [steps/s] (`minStartSpeedSps = 650.0`)
- `vend`: 終了速度 [steps/s] (`minStartSpeedSps = 650.0`)

#### ステップ1: 加速・減速ステップ数の計算

```cpp
// 加速に必要なステップ数 (v0 → vtar)
accelStepsF = (vtar*vtar - v0*v0) / (2.0f * a)

// 減速に必要なステップ数 (vtar → vend)
decelStepsF = (vtar*vtar - vend*vend) / (2.0f * a)
```

**物理式**: v² = v₀² + 2as より導出

#### ステップ2: 台形 or 三角の判定

```cpp
if (total >= accelSteps + decelSteps) {
    // 台形プロファイル
    cruiseSteps = total - accelSteps - decelSteps
    peak = vtar  // 目標速度に到達可能
} else {
    // 三角プロファイル
    // 到達可能なピーク速度を再計算
    s = (a * total) + (v0*v0 + vend*vend) * 0.5f
    peak = sqrt(s)
    
    // 再算出
    accelSteps = (peak*peak - v0*v0) / (2.0f * a)
    decelSteps = total - accelSteps
    cruiseSteps = 0
}
```

#### ステップ3: 計画変数への格納

```cpp
planActive[idx] = true;
planTotalSteps[idx]  = total;
planStepsDone[idx]   = 0;
planAccelSteps[idx]  = accelSteps;
planCruiseSteps[idx] = cruiseSteps;
planDecelSteps[idx]  = decelSteps;
planPeakSpeedSps[idx]= peak;
```

### 計算例

#### 例1: 台形プロファイル
- 総ステップ数: 2000
- 目標速度: 1333.3 steps/s (200rpm)
- 加速度: 3416.5 steps/s²
- 開始/終了速度: 650 steps/s

```
加速ステップ数 = (1333.3² - 650²) / (2 × 3416.5) ≈ 198 steps
減速ステップ数 = (1333.3² - 650²) / (2 × 3416.5) ≈ 198 steps
等速ステップ数 = 2000 - 198 - 198 = 1604 steps

→ 台形プロファイル
```

#### 例2: 三角プロファイル
- 総ステップ数: 200
- 目標速度: 1333.3 steps/s
- 加速度: 3416.5 steps/s²

```
加速+減速 = 198 + 198 = 396 steps > 200 steps

→ 三角プロファイルに切り替え

ピーク速度 = sqrt((3416.5 × 200) + (650² + 650²) / 2) ≈ 886 steps/s
加速ステップ数 ≈ 82 steps
減速ステップ数 ≈ 118 steps
等速ステップ数 = 0 steps
```

---

## 割り込み処理による速度制御

### 処理タイミング
タイマー割り込み（ISR）内で各ステップパルスの立ち上がり時に実行されます。

### handleStep関数の処理フロー（728-791行目）

```
handleStep(idx) {
  ステップパルストグル処理
    ↓
  stepHigh == true の場合
    ↓
  remainingSteps をデクリメント
  planStepsDone をインクリメント
    ↓
  台形加速が有効か？
    ↓ YES
  updateTrapezoidSpeed(idx) を呼び出し
    ↓
  速度更新完了
}
```

### updateTrapezoidSpeed関数（879-952行目）

#### 処理の流れ

1. **パラメータ取得**
```cpp
float accel = accelerationSps2[idx];     // 加速度
float dt = stepInterval[idx] / 2000000.0f; // 半周期時間 [s]
```

2. **動作モード判定**

##### ケース1: 無限動作 (`remainingSteps == 0`)
```cpp
// 目標速度まで加速
if (currentSpeedSps[idx] < targetSpeedSps[idx]) {
    currentSpeedSps[idx] += accel * dt;
    if (currentSpeedSps[idx] > targetSpeedSps[idx]) {
        currentSpeedSps[idx] = targetSpeedSps[idx];
    }
}
```

##### ケース2: 事前計画動作 (`planActive == true`)

**加速フェーズ** (`planStepsDone < planAccelSteps`)
```cpp
if (currentSpeedSps[idx] < planPeakSpeedSps[idx]) {
    currentSpeedSps[idx] += accel * dt;
    if (currentSpeedSps[idx] > planPeakSpeedSps[idx]) {
        currentSpeedSps[idx] = planPeakSpeedSps[idx];
    }
}
```

**等速フェーズ** (`planAccelSteps ≤ planStepsDone < planAccelSteps + planCruiseSteps`)
```cpp
currentSpeedSps[idx] = planPeakSpeedSps[idx];
```

**減速フェーズ** (`planStepsDone ≥ planAccelSteps + planCruiseSteps`)
```cpp
currentSpeedSps[idx] -= accel * dt;
if (currentSpeedSps[idx] < minStartSpeedSps) {
    currentSpeedSps[idx] = minStartSpeedSps;
}
```

##### ケース3: 動的判断（フォールバック）
```cpp
// 停止までに必要なステップ数を計算
stepsToStop = (currentSpeedSps² ) / (2 * accel);

if (remainingSteps <= stepsToStop) {
    // 減速開始
    currentSpeedSps[idx] -= accel * dt;
} else {
    // 加速継続
    currentSpeedSps[idx] += accel * dt;
}
```

3. **インターバル更新**
```cpp
unsigned int newInterval = spsToIntervalUs(currentSpeedSps[idx]);
if (newInterval > 0 && newInterval != stepInterval[idx]) {
    stepInterval[idx] = newInterval;
    updateTimerOCR(idx);  // タイマーレジスタを更新
}
```

### 速度-インターバル変換

```cpp
// steps/s → μs 変換
unsigned int spsToIntervalUs(float sps) {
    if (sps <= 0.0f) return 0;
    return (unsigned int)(1000000.0f / sps);
}

// 例: 1333.3 steps/s → 750 μs
```

### タイマーレジスタ更新（794-801行目）

```cpp
inline void updateTimerOCR(int idx) {
    uint16_t ocrValue = (uint16_t)((16UL * stepInterval[idx] / 2UL) - 1UL);
    switch (idx) {
        case 0: OCR3A = ocrValue; break;  // モーター1
        case 1: OCR4A = ocrValue; break;  // モーター2
        case 2: OCR5A = ocrValue; break;  // モーター3
    }
}
```

**計算式**: OCR値 = (16MHz × interval[μs] / 2) - 1

---

## コマンド処理

### Aコマンド: 台形加速ON/OFF (1370-1391行目)

#### フォーマット
```
STX + ポンプNo + 'A' + 6桁データ + CS + ETX
```

#### データ値
- `000000`: 台形加速OFF
- `000001`: 台形加速ON（その他の値でもON）

#### 処理内容

**台形加速OFFの場合**
```cpp
useTrapezoid[idx] = false;
currentSpeedSps[idx] = targetSpeedSps[idx];  // 等速に即時切替
planActive[idx] = false;                     // 事前計画を無効化
usePrecomputed[idx] = false;                 // 事前計算配列を無効化
```

**台形加速ONの場合**
```cpp
useTrapezoid[idx] = true;
usePrecomputed[idx] = false;  // 事前計算配列は使用しない
// Mコマンドで事前計画が設定される
```

### Vコマンド: 速度設定 (1326-1347行目)

#### フォーマット
```
STX + ポンプNo + 'V' + 6桁rpm + CS + ETX
```

#### 処理内容

**台形加速ONの場合**
```cpp
targetSpeedSps[idx] = rpmToSps(rpm);

// 0.2秒で新しい目標速度へ到達するよう加速度を更新
float base = (currentSpeedSps[idx] > 1.0f) ? currentSpeedSps[idx] : minStartSpeedSps;
if (targetSpeedSps[idx] > base) {
    float dv = targetSpeedSps[idx] - base;
    accelerationSps2[idx] = dv / targetRampTimeSec;  // 0.2秒で到達
    if (accelerationSps2[idx] < 1.0f) {
        accelerationSps2[idx] = 1.0f;
    }
}
```

**台形加速OFFの場合**
```cpp
unsigned int newInterval = rpmToIntervalUs(rpm);
stepInterval[idx] = newInterval;
pendingIntervalUs[idx] = newInterval;
pendingIntervalUpdate[idx] = true;  // 次の割り込みで更新
targetSpeedSps[idx] = rpmToSps(rpm);
```

### Mコマンド: モーター開始 (1176-1283行目)

#### フォーマット
```
STX + ポンプNo + 'M' + 6桁ステップ数 + CS + ETX
```

#### データ値
- `000000`: 無限動作
- `000001〜`: 指定ステップ数で動作（内部で×2倍される）

#### 処理フロー

1. **事前計画の設定** (1177-1228行目)
```cpp
if (useTrapezoid[idx] && value > 0 && targetSpeedSps[idx] > 0.0f) {
    // 台形/三角プロファイルを計算
    // planActive, planAccelSteps, planCruiseSteps, planDecelSteps を設定
}
```

2. **バルブ制御判定** (1231-1283行目)
   - `valveNormallyOpen[idx] == true`: モーター即座開始
   - `valveNormallyOpen[idx] == false`: バルブ開放 → 0.5秒後 → モーター開始

3. **台形加速設定** (1238-1264行目)
```cpp
if (useTrapezoid[idx]) {
    currentSpeedSps[idx] = minStartSpeedSps;  // 最小速度から開始
    
    // 目標速度の検証
    if (targetSpeedSps[idx] < 1.0f) {
        targetSpeedSps[idx] = rpmToSps(200);  // デフォルト200rpm
    }
    
    // 加速度の計算
    if (targetSpeedSps[idx] > currentSpeedSps[idx]) {
        float dv = targetSpeedSps[idx] - currentSpeedSps[idx];
        accelerationSps2[idx] = dv / targetRampTimeSec;
    }
    
    // 事前計算配列は使用しない（重要）
    usePrecomputed[idx] = false;
    
    // タイマー初期化
    stepInterval[idx] = spsToIntervalUs(currentSpeedSps[idx]);
    setupTimer(idx, stepInterval[idx]);
}
```

### Sコマンド: モーター停止 (1288-1316行目)

#### 処理内容
```cpp
motorEnabled[idx] = false;
if (!excitationAlwaysOn[idx]) {
    digitalWrite(enaPins[idx], HIGH);  // 励磁OFF
}
remainingSteps[idx] = 0;
currentSpeedSps[idx] = 0.0f;
planActive[idx] = false;             // 事前計画を無効化
planStepsDone[idx] = 0;
resetPrecomputedArray(idx);          // 事前計算配列をリセット
```

---

## 事前計算配列（無効化）

### 背景
以前は起動時に事前計算配列（`precomputedIntervals[3][600]`）を使用していましたが、動的な事前計画とのステップ数不整合により、加速が2回実行される問題が発生しました。

### 事前計算配列の仕様（参考情報）

#### 初期化（805-843行目）
```cpp
void initializeTrapezoidArrays() {
    float peakSpeed = 1333.3f;  // 200rpm
    float accel = (peakSpeed - minStartSpeedSps) / targetRampTimeSec;
    
    for (int idx = 0; idx < 3; idx++) {
        for (int step = 0; step < MAX_TRAPEZOID_STEPS; step++) {
            float timeRatio = (float)step / (float)MAX_TRAPEZOID_STEPS;
            currentSpeed = minStartSpeedSps + (peakSpeed - minStartSpeedSps) * timeRatio;
            precomputedIntervals[idx][step] = spsToIntervalUs(currentSpeed);
        }
        precomputedStepCount[idx] = MAX_TRAPEZOID_STEPS;
    }
}
```

### 無効化の理由

1. **ステップ数不整合**
   - 事前計算配列: 600ステップ固定
   - 事前計画: 動的計算（例: 198ステップ）
   - 結果: 加速が2回実行される

2. **柔軟性の欠如**
   - 固定プロファイル（200rpm、0.2秒のみ）
   - 異なる速度や加速時間に対応できない

3. **解決策**
   - 動的な事前計画のみを使用
   - より柔軟で正確な制御を実現

### 現在の実装
```cpp
// Mコマンド内（1262-1264行目）
// 事前計算配列は使用せず、動的な事前計画のみを使用
usePrecomputed[idx] = false;

// Aコマンド内（1384-1386行目）
// 台形加速ON時も事前計算配列は使用しない
usePrecomputed[idx] = false;
```

---

## 制限事項と注意点

### 1. 最小開始速度の制約
- **制約**: すべての動作は `minStartSpeedSps = 650 steps/s` 以上で開始
- **理由**: ステッピングモーターの脱調防止
- **影響**: 目標速度が650 steps/s未満の場合、調整が必要

### 2. 加速時間の目標値
- **目標**: `targetRampTimeSec = 0.2秒`
- **実際**: 加速度は動的に計算されるため、常に0.2秒とは限らない
- **計算式**: `加速度 = (目標速度 - 開始速度) / 0.2秒`

### 3. 浮動小数点演算の精度
- **精度**: 割り込み処理内で浮動小数点演算を使用
- **パフォーマンス**: Arduino Mega (16MHz) では十分な速度
- **注意**: より高速な制御が必要な場合は固定小数点演算への変更を検討

### 4. 三角プロファイルへの自動切替
- **条件**: `総ステップ数 < 加速ステップ数 + 減速ステップ数`
- **動作**: 自動的に到達可能なピーク速度を再計算
- **注意**: 目標速度に到達しない場合がある

### 5. 無限動作の制御
- **特徴**: `remainingSteps = 0` で無限動作
- **動作**: 目標速度まで加速後、等速を維持
- **停止**: Sコマンドまたは緊急停止で停止

### 6. 割り込み処理の最適化
- **最適化**: 2回に1回のみ速度計算を実行（784-787行目）
```cpp
static uint8_t calcCounter[3] = {0, 0, 0};
calcCounter[idx] = (calcCounter[idx] + 1) & 0x01;
if (calcCounter[idx] == 0) {
    updateTrapezoidSpeed(idx);
}
```
- **理由**: 割り込み処理時間の短縮

### 7. 速度変更の制約
- **Vコマンド**: 動作中でも速度変更可能
- **動作**: 新しい目標速度に向けて0.2秒で加減速
- **注意**: 急激な速度変更は脱調のリスクあり

---

## 付録

### A. 速度単位変換

#### RPM → steps/s
```cpp
float rpmToSps(long rpm) {
    return (float)rpm * (float)stepsPerRev / 60.0f;
}

// 例: 200 rpm → 200 × 400 / 60 = 1333.3 steps/s
```

#### steps/s → μs (インターバル)
```cpp
unsigned int spsToIntervalUs(float sps) {
    return (unsigned int)(1000000.0f / sps);
}

// 例: 1333.3 steps/s → 1000000 / 1333.3 = 750 μs
```

#### RPM → μs (直接変換)
```cpp
unsigned int rpmToIntervalUs(long rpm) {
    float pps = (rpm * stepsPerRev) / 60.0;
    return (unsigned int)(1000000.0 / pps);
}
```

### B. 物理式の導出

#### 等加速度運動の公式
```
v = v₀ + at           (速度)
s = v₀t + (1/2)at²   (変位)
v² = v₀² + 2as       (速度と変位の関係)
```

#### 加速ステップ数の導出
```
v² = v₀² + 2as  より
s = (v² - v₀²) / (2a)

v: 目標速度 [steps/s]
v₀: 開始速度 [steps/s]
a: 加速度 [steps/s²]
s: ステップ数
```

### C. デバッグ方法

#### 1. 速度プロファイルの確認
```cpp
// updateTrapezoidSpeed内に追加
Serial.print("Speed: ");
Serial.print(currentSpeedSps[idx]);
Serial.print(" Phase: ");
if (planStepsDone[idx] < planAccelSteps[idx]) {
    Serial.println("ACCEL");
} else if (planStepsDone[idx] < planAccelSteps[idx] + planCruiseSteps[idx]) {
    Serial.println("CRUISE");
} else {
    Serial.println("DECEL");
}
```

#### 2. 事前計画の確認
```cpp
// Mコマンド処理後に追加
Serial.print("Total: "); Serial.println(planTotalSteps[idx]);
Serial.print("Accel: "); Serial.println(planAccelSteps[idx]);
Serial.print("Cruise: "); Serial.println(planCruiseSteps[idx]);
Serial.print("Decel: "); Serial.println(planDecelSteps[idx]);
Serial.print("Peak: "); Serial.println(planPeakSpeedSps[idx]);
```

### D. トラブルシューティング

| 症状 | 原因 | 対処法 |
|------|------|--------|
| モーターが脱調する | 加速度が高すぎる | `targetRampTimeSec`を増やす（例: 0.3秒） |
| 加速が滑らかでない | 割り込み周期が不適切 | `stepInterval`の最小値を確認 |
| 速度が目標に達しない | 三角プロファイルになっている | ステップ数を増やすか目標速度を下げる |
| 停止時に音が出る | 最小速度が高い | `minStartSpeedSps`を下げる（例: 500 steps/s） |
| 加速が2回実行される | 事前計算配列が有効 | `usePrecomputed[idx] = false`を確認 |

---

## 改訂履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| 1.0 | 2025/10/08 | 初版作成 |
| - | - | 事前計算配列の無効化を反映 |
| - | - | 二重加速問題の修正を文書化 |

---

**文書作成者**: AI Assistant  
**レビュー**: 必要に応じて更新してください

