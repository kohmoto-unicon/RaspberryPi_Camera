# シリアル通信バッファ問題の修正

## 問題概要

Tコマンド（トータル回転数取得）で、Arduino側が正しい応答を送信しているのに、Python側で1個前のデータ（古いデータ）を表示していた。

### 原因

シリアルバッファに前回のコマンドの応答やゴミデータが残っている状態で、新しいコマンドを送信すると、古いデータを読んでしまう。

```
【問題のシーケンス】
1. Tコマンド送信（ポンプ1の回転数=100回転の応答）
   → Python側が読み込み成功
   
2. Tコマンド送信（ポンプ1の回転数=150回転の応答を期待）
   → 【問題】バッファに前回の「100回転」がまだ残っている
   → Python側が古い「100回転」を読み込む
   → 実際の「150回転」はバッファに残る
   
3. Tコマンド送信（ポンプ1の回転数=200回転の応答を期待）
   → 【問題】バッファには「150回転」が残っている
   → Python側が前回の「150回転」を読み込む
```

## 修正内容

### 1. シリアルバッファの事前クリア

**すべての取得コマンド送信前**に受信バッファをクリアする処理を追加

修正対象API:
- `api_get_current()` (Cコマンド)
- `api_get_rpm()` (Xコマンド)
- `api_get_total_revolutions()` (Tコマンド)
- `api_get_control_status()` (Jコマンド)

**実装コード例:**
```python
# ===== 重要: シリアルバッファをクリア（古いデータを除去） =====
if target_ser.in_waiting > 0:
    old_data = target_ser.read(target_ser.in_waiting)
    if DEBUG_SERIAL_LOG:
        print(f"[{port_name}] 古いバッファデータをクリア: {old_data.hex()} ({len(old_data)} bytes)")

# コマンド送信
success = send_serial_command(pump, "T", "000000")
```

### 2. デバッグログの改善

バッファクリアが実行されたかどうかを確認できるようにログを追加

```python
if DEBUG_SERIAL_LOG:
    print(f"[{port_name}] Tコマンド送信完了。応答待機中...")
    # ... 応答受信後 ...
    hex_str = ' '.join([f'{b:02X}' for b in response])
    print(f"[{port_name}] Tコマンド応答受信: {hex_str} ({len(response)} bytes)")
```

これにより、以下が可能に:
- コマンド送信→応答受信の流れが明確になる
- 古いバッファデータがクリアされたかどうかが確認できる
- トラブル時の原因特定が容易になる

## 修正後の効果

✅ **シリアルバッファが常に新しい応答データのみを含む**
✅ **連続コマンド送信時も正確なデータが取得できる**
✅ **デバッグログで問題の追跡が可能**

## 設定方法

### デバッグログを有効にする場合

`app.py` の先頭で以下を設定:

```python
DEBUG_SERIAL_LOG = True  # シリアル通信の送受信ログ
```

### ログ出力例

```
[ACM0(COM18)] 古いバッファデータをクリア: 0202003144303030303003 (11 bytes)
[ACM0(COM18)] Tコマンド送信完了。応答待機中...
[ACM0(COM18)] Tコマンド応答受信: 02 31 30 30 41 42 43 39 F3 03 (10 bytes)
```

## テスト方法

1. 同じポンプへのTコマンドを連続実行
2. 返却される回転数が常に最新値であることを確認
3. 古い値が表示されないことを確認

## 今後の推奨事項

- [ ] シリアル通信の再試行メカニズムを追加（チェックサムエラー時）
- [ ] コマンド/応答のタイムスタンプ記録
- [ ] マルチポート環境でのバッファ独立管理を確認
