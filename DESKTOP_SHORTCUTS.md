# デスクトップショートカット ガイド

## 📱 作成されたショートカット

デスクトップに以下の7つのショートカットアイコンが作成されました：

### 1. 🎥 カメラ起動
- **ファイル名**: `camera-start.desktop`
- **機能**: カメラアプリケーションを起動し、ハイセラポンプ制御画面を自動的に開きます
- **実行内容**: `app start` + ハイセラポンプ制御画面を開く
- **アイコン**: 📹 カメラアイコン

ダブルクリックすると、ターミナルが開いてカメラアプリが起動し、起動状態を確認後、自動的にブラウザでハイセラポンプ制御画面（http://localhost:5000/pump_control）が開きます。

### 2. 🛑 カメラ停止
- **ファイル名**: `camera-stop.desktop`
- **機能**: カメラアプリケーションを停止します
- **実行内容**: `app stop`
- **アイコン**: 🛑 停止アイコン

ダブルクリックすると、ターミナルが開いてカメラアプリが停止し、停止状態を確認して表示します。

### 3. 🔄 カメラ再起動
- **ファイル名**: `camera-restart.desktop`
- **機能**: カメラアプリケーションを再起動します
- **実行内容**: `app restart`
- **アイコン**: 🔄 リフレッシュアイコン

コードを変更した後など、アプリを再起動したい時に使用します。

### 4. ℹ️ カメラ状態確認
- **ファイル名**: `camera-status.desktop`
- **機能**: カメラアプリケーションの状態を確認します
- **実行内容**: `app status`
- **アイコン**: ℹ️ 情報アイコン

アプリが動作しているか、停止しているかを確認できます。

### 5. 🌐 カメラWebページを開く
- **ファイル名**: `camera-browser.desktop`
- **機能**: ブラウザでカメラストリーミングページを開きます
- **URL**: http://localhost:5000
- **アイコン**: 🌐 ブラウザアイコン

カメラアプリが起動している状態でダブルクリックすると、ブラウザでカメラ映像が表示されます。

### 6. 🔧 ハイセラポンプ制御
- **ファイル名**: `pump-control.desktop`
- **機能**: ブラウザでハイセラポンプ制御画面を開きます
- **URL**: http://localhost:5000/pump_control
- **アイコン**: 🔧 システムモニターアイコン

カメラアプリが起動している状態でダブルクリックすると、ブラウザでハイセラポンプ制御画面が表示されます。ポンプ1-6の制御が可能です。

### 7. 💉 シリンジポンプ制御
- **ファイル名**: `syringe-pump.desktop`
- **機能**: ブラウザでシリンジポンプ制御画面を開きます
- **URL**: http://localhost:5000/syringe_pump
- **アイコン**: ⚙️ システム設定アイコン

カメラアプリが起動している状態でダブルクリックすると、ブラウザでシリンジポンプ制御画面が表示されます。

## 📋 使い方

### 基本的な操作フロー

#### ワンクリックで起動（推奨）

1. **カメラを起動**: 「カメラ起動」アイコンをダブルクリック
   - カメラアプリが起動します
   - 自動的にハイセラポンプ制御画面がブラウザで開きます
2. **完了**: すぐにポンプ制御が可能です！

#### 個別に開く場合

1. **カメラを起動**: 「カメラ起動」アイコンをダブルクリック
2. **別の画面を開く**: 
   - カメラ映像: 「カメラWebページを開く」アイコン
   - ハイセラポンプ: 「ハイセラポンプ制御」アイコン
   - シリンジポンプ: 「シリンジポンプ制御」アイコン

### 状態確認

- **アプリが起動しているか確認**: 「カメラ状態確認」アイコンをダブルクリック
- **Active: active (running)** → 起動中
- **Active: inactive (dead)** → 停止中

### コード変更後

1. **カメラを再起動**: 「カメラ再起動」アイコンをダブルクリック
2. **変更が反映されます**

### カメラを停止したい時

- **カメラを停止**: 「カメラ停止」アイコンをダブルクリック

## 🔧 カスタマイズ

### アイコンの位置を変更

デスクトップ上でアイコンをドラッグ＆ドロップして、好きな位置に配置できます。

### アイコンの削除

不要なアイコンを右クリックして「削除」または「ゴミ箱に移動」を選択します。

### アイコンの再作成

誤って削除した場合は、以下のディレクトリからコピーできます：

```bash
# デスクトップファイルの場所
~/Desktop/camera-*.desktop
```

または、以下のコマンドで再作成できます：

```bash
cd /home/kohmoto/projects/RaspberryPi_Camera
# 必要に応じてドキュメントを参照
```

## 💡 ヒント

### ショートカットが動作しない場合

1. **実行権限を確認**:
   ```bash
   ls -l ~/Desktop/camera-*.desktop
   ```
   `-rwxr-xr-x` のように `x` (実行権限) があることを確認

2. **権限を再設定**:
   ```bash
   chmod +x ~/Desktop/camera-*.desktop
   ```

3. **信頼済みとしてマーク**:
   ```bash
   gio set ~/Desktop/camera-start.desktop metadata::trusted true
   gio set ~/Desktop/camera-stop.desktop metadata::trusted true
   gio set ~/Desktop/camera-restart.desktop metadata::trusted true
   gio set ~/Desktop/camera-status.desktop metadata::trusted true
   gio set ~/Desktop/camera-browser.desktop metadata::trusted true
   ```

### ターミナルが開いたまま残る

デスクトップファイル内で `read` コマンドを使用しているため、Enterキーを押すまでターミナルは閉じません。これにより、実行結果を確認できます。

### 自動的に閉じるようにしたい場合

デスクトップファイルを編集して、`&& read` の部分を削除します：

```bash
nano ~/Desktop/camera-start.desktop
```

## 📁 ファイルの場所

すべてのデスクトップショートカットは以下の場所にあります：

```
/home/kohmoto/Desktop/
├── camera-start.desktop      # カメラ起動（自動でハイセラポンプ制御画面を開く）
├── camera-stop.desktop       # カメラ停止
├── camera-restart.desktop    # カメラ再起動
├── camera-status.desktop     # カメラ状態確認
├── camera-browser.desktop    # カメラ映像Webページを開く
├── pump-control.desktop      # ハイセラポンプ制御画面を開く
└── syringe-pump.desktop      # シリンジポンプ制御画面を開く
```

## 🎨 アイコンのカスタマイズ

デスクトップファイルの `Icon=` 行を編集することで、アイコンを変更できます。

利用可能なアイコン名の例：
- `camera-video` - カメラアイコン
- `process-stop` - 停止アイコン
- `view-refresh` - 更新アイコン
- `dialog-information` - 情報アイコン
- `web-browser` - ブラウザアイコン
- `applications-multimedia` - マルチメディアアイコン
- `emblem-photos` - 写真アイコン

### アイコンの変更方法

```bash
nano ~/Desktop/camera-start.desktop
```

`Icon=camera-video` の行を他のアイコン名に変更して保存します。

---

**作成日**: 2025-10-10  
**バージョン**: 1.0

